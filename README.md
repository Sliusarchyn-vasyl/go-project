# go-project

A minimal Go CLI application that prints **"Hello, World!"** to standard output.

## Requirements
- Go 1.22+ (recommended)
- Git

## Quick Start
Run directly:
```bash
go run .
# or
go run main.go
# my project
## Build (Compile)
go build -o dist/hello .
./dist/hello
